mixin breadcrumbs(links = [])
  .breadcrumbs
    a(href='/').breadcrumbs__icon-link
    if links.length !== 0 
      .breadcrumbs__container
        each value, index in links 
          +link(value.sort, value.href, index, value.text, 'crumb', links)
          if index < links.length - 1
            span.span /