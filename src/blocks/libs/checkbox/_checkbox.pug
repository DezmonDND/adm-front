mixin checkbox(options)
    input(type=`${options.sort}` id=`${options.id}` name=`${options.name}` class=`сheckbox ${options.sort === 'radio' ? 'radio' : ''}` checked=options.status)
    label(for=`${options.id}`)