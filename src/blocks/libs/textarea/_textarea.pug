mixin textarea(options, error)
    .container
        textarea(class=`textarea textarea-${options.size}` placeholder=options.placeholder oninput='getLength(this)')
        .counter
            span.current 0
            span /
            span.max 900