include ../buttons/_buttons

mixin search-button(size, event)
    +button({ type: 'button', class: `button_blue search__button search__button_${size}`, icon: 'add-outline', event: '1', icon: 'search-outline'})     