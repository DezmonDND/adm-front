mixin input(input_options, error)
    input(placeholder=input_options.placeholder,
        class=`input input-${input_options.size} input-${input_options.type} input-search-${input_options.size} ${error ? 'input-error' : '' }`)